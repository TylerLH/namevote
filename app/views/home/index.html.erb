<div class="latest_names">
	<h3>All Names (Newest to Oldest)</h3>
	<ul>
	<%- @names.each do |n| %>
		<li><%= link_to n.name.to_s.titleize, n %> // <%= n.votes %> Votes <%= link_to "I Like This!", vote_path(n), :class => "vote_link" %></li>
	<% end %>
	</ul>
	<%= will_paginate @names %>
</div>

	<p>Don't forget to check your name's availability. Use this tool: <%= link_to "Domai.nr", "http://domai.nr" %></p>
	<p>You can also comment on a name by clicking on it to view details.</p>

<div class="add_name">
<h3>Got a name to add? Enter it below.</h3>
	<%= form_tag('/names', :remote => true) do -%>
		<%= label_tag(:name, "Possible Name") -%>
		<%= text_field_tag(:name) -%>
		<%= submit_tag("Add Name", :class => "add_button", :disable_with => "Adding...") -%>
	<% end -%>
</div>

<ol class="top_ten">
<%- @topten.each do |tn| %>
	<li><%= link_to tn.name, tn %> // <%= tn.votes %> Votes <%= link_to "I Like This!", vote_path(tn), :class => "vote_link" %></li>
<% end %>
</ol>